import React from "react";

// console.log(logo);

const Header = () => {
    return (
        <header className="header">
            <section>
                
            </section>
        </header>
    );
};

export default Header;