import React from "react";
import Nav from "../Nav/Nav";

// console.log(logo);

const Header = () => {
    return (
        <header className="container">
            <section>
                <Nav />
            </section>
        </header>
    );
};

export default Header;